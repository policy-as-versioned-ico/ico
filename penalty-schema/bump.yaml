# The declared bump for the NEXT penalty-schema release (ticket 04 Answer 3,
# re-grill 13): one file per feed, beside rule.yaml, so "the next release" is
# one unambiguous value. A human reviews it in the PR before cut-release.yml
# tags.
#
# History of this file is in git; the two corrections it used to carry in prose
# (2026-08-29 ticket 43, 2026-09-06 ticket 67) are both spent -- v3.0.0 is tagged
# and `git diff v3.0.0 origin/main -- penalty-schema` was empty when it was last
# reset to `none`.
#
# SET TO `major` 2026-09-09 (eco-system ticket 79 items 1, 3 and 5).
# penalty-schema/v4/ is now in the tree and untagged: the queued release is
# v4.0.0 and it is a major, because the payload schema changed (ADR-0019).
# What changed: every real example carries `status`, `final_as_of` and
# `litigation`; every violation type publishes a `frequency` with a dated
# `basis`; every control weight carries the same `basis`.
#
# THE CUT IS NOT DONE HERE. `.github/workflows/cut-release.yml` is dispatched by
# the owner, and it is the owner who signs v4.0.0. This file declares the bump
# the gate then computes against the tag; the gate
# (.github/scripts/declared-bump-gate.py, eco-system ticket 103) reads the
# predecessor AT v3.0.0, checks that v3 on disk is byte-for-byte what v3.0.0
# published, and refuses any tag that is not bump(v3.0.0, major) = v4.0.0.
# RESET TO `none` 2026-09-10: v4.0.0 is tagged, signed and published, and nothing
# further is queued. The line above records what v4.0.0 was and why; this file's
# own history is the record, so the prose is kept rather than deleted.
bump: none
